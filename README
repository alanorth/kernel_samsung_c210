To use:

$ export CROSS_COMPILE=path/to/arm/compiler/ending/in-
$ make p6810_defconfig
$ make -jN CONFIG_INITRAMFS_SOURCE=path/to/initramfs/root

p6810 can be replaced with one of the following other devices:

p6800
p6810
